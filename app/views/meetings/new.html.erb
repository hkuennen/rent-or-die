<div class="banner" style="background-image: url(<%= cl_image_path @survivalist.user.photo.key %>">
</div>

<div class="container">
  <div class="row justify-content-center mt-3 mb-5">
    <div class="col-12 col-lg-6">
      <%= link_to "Back to all Survivalists", survivalists_path, class: "btn btn-primary mb-2" %>
    </div>
  </div>
</div>

<div class="container">
  <div class="row justify-content-center mt-1 mb-5">
    <div class="col-12 col-lg-6">
      <h1 class="text-light">Reserve <%= @survivalist.name %></h1>
      <%= simple_form_for [@survivalist, @meeting] do |f| %>
        <%= f.input :start_date, as: :datetime, html5: true %>
        <%= f.input :end_date, as: :datetime, html5: true %>
        <div class="btn btn-danger">
          <%= f.button :submit, class:"text-light" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
