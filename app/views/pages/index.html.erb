
<div class="p-3 mb-2 bg-dark text-light">
<div class="gradient">
<div class="container">
  <h1 class="text-center mt-5">	&darr; My Survivalists! &darr;</h2>
  <div class="my-5">
    <div class="cards">
        <% @meetings.each do |meeting, survivalists| %>
        <div class="card-trip">
          <img src="<%= meeting.survivalist.photo %>">
          <div class="card-trip-infos">
            <h2><%= meeting.survivalist.name %></h2>
            <p><%= meeting.survivalist.category %></p>
            <p><%= meeting.survivalist.skills %></p>
            <p>From <em><%= meeting.start_date %></em> to <em><%=meeting.end_date %></em></p>
            <p>£222999.99/day<</p>
          </div>
        </div>
        <% end %>
    </div>
  </div>
</div>
</div>
</div>
